# Procédure de Test — Milestone v5.7.1

**Version** : 5.7.1
**Date** : 2026-06-07
**Testeur** : QA
**Issues** : #97 (POST /api/categories), #98 (NORMAL → SPEEDY), #99 (param medias)

---

## Prérequis

- [ ] Environnement : LOCAL ou QUALIF
- [ ] Serveur démarré : `./buzzcontrol-qualif` (ou `server.exe` Windows)
- [ ] Accès : navigateur sur `http://localhost`
- [ ] Outil : `curl` disponible pour les tests API bas niveau

---

## Scénario 1 — Bouton + catégorie : création depuis QuestionsPage (#97)

**Objectif** : Vérifier que l'utilisateur peut créer une catégorie custom directement depuis QuestionsPage et qu'elle est auto-sélectionnée.

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Naviguer vers `/questions` | Page chargée, sélecteur de catégories visible | | |
| 2 | Observer le sélecteur de catégories | Un bouton "+" (discret, bordure dashed) est présent à la fin des catégories | | |
| 3 | Cliquer sur "+" | Un formulaire inline apparaît avec un champ texte, un bouton "Valider" et un bouton "Annuler" | | |
| 4 | Saisir "Sport Extrême" dans le champ | Le texte s'affiche dans le champ | | |
| 5 | Cliquer sur "Valider" | Le formulaire se ferme, la catégorie "Sport Extrême" apparaît dans le sélecteur, elle est **auto-sélectionnée** (active) | | |
| 6 | Vérifier via `curl -s http://localhost/api/categories` | La réponse JSON contient `{"key":"SPORT_EXTREME","name":"Sport Extrême","imageURL":"","isCustom":true}` | | |
| 7 | Cliquer sur "+" à nouveau et saisir "Sport Extrême" | Cliquer sur "Valider" → message d'erreur "Cette catégorie existe déjà" s'affiche | | |
| 8 | Cliquer sur "Annuler" | Le formulaire se ferme, le bouton "+" est à nouveau visible | | |

**Cas limite — nom vide** :

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 9 | Cliquer "+", ne rien saisir, cliquer "Valider" | Message "Nom invalide" s'affiche (validation frontend) | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 2 — Renommage "Normal" → "Speedy" (#98)

**Objectif** : Vérifier l'affichage du type "Speedy" et la migration transparente des fichiers existants.

### 2a — Interface QuestionsPage

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Naviguer vers `/questions` | Page chargée | | |
| 2 | Observer le sélecteur de type | Le bouton "Speedy" existe (remplace l'ancien "Normal"), il est **actif par défaut** | | |
| 3 | Cliquer "QCM" puis re-cliquer "Speedy" | Speedy devient actif, QCM devient inactif | | |
| 4 | Créer une question (formulaire Speedy) puis l'enregistrer | Succès, la question s'affiche dans la liste | | |

### 2b — Migration des fichiers existants (rétrocompatibilité)

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 5 | Créer manuellement un fichier `data/files/questions/99/question.json` avec `"TYPE":"NORMAL"` | Fichier créé | | |
| 6 | Appeler `curl -s http://localhost/questions \| python3 -m json.tool` | La question 99 est retournée avec `"TYPE": "SPEEDY"` (migration automatique) | | |
| 7 | Vérifier que le fichier sur disque n'est PAS modifié | Le fichier contient toujours `"TYPE":"NORMAL"` (migration en lecture seulement) | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 3 — Backup sélectif avec param `medias` (#99)

**Objectif** : Vérifier que le paramètre `medias=true` (qui remplace `backgrounds`) inclut backgrounds + categories dans le TAR.

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Ajouter un fond via BackupPage ou directement dans `data/files/backgrounds/` | Fond présent | | |
| 2 | Créer une catégorie custom via bouton "+" dans QuestionsPage | Catégorie présente dans `data/files/categories/` | | |
| 3 | Naviguer vers `/backup` | BackupPage affichée | | |
| 4 | Décocher toutes les options sauf "Médias & Catégories", cliquer "Sauvegarder" | Téléchargement d'un fichier `.tar` | | |
| 5 | Inspecter le TAR : `tar -tvf <fichier>.tar` | Le TAR contient `files/backgrounds/...` ET `files/categories/...`, mais pas `files/questions/` ni `config/` | | |
| 6 | Appel direct : `curl -s "http://localhost/backup-select?medias=true" -o test.tar && tar -tvf test.tar` | Même résultat qu'étape 5 | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 4 — Reset sélectif avec param `medias` (#99)

**Objectif** : Vérifier que le reset sélectif avec `medias=true` supprime backgrounds et categories.

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | S'assurer qu'il y a des fonds ET des catégories custom | Confirmé | | |
| 2 | Naviguer vers `/backup`, section "Réinitialisation" | Page affichée | | |
| 3 | Cocher uniquement "Médias & Catégories", cliquer "Réinitialiser" | Confirmation demandée | | |
| 4 | Confirmer | Succès, message "Réinitialisation effectuée" | | |
| 5 | Vérifier : `curl -s "http://localhost/api/categories"` | Seules les catégories hardcodées sont retournées (isCustom=false), plus de custom | | |
| 6 | Vérifier que les questions ne sont PAS supprimées | `curl -s http://localhost/questions` retourne les questions intactes | | |

**Test API direct** :
```bash
curl -s "http://localhost/reset-select?medias=true" | python3 -m json.tool
# Attendu : {"status":"ok","reset":{"medias":true}}
```

| Résultat obtenu | OK ? |
|----------------|------|
| `{"status":"ok","reset":{"medias":true}}` | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 5 — Smoke tests API v5.7.1

**Objectif** : Vérifier les endpoints clés en moins de 5 minutes.

```bash
# 5.1 — GET /api/categories
curl -s http://localhost/api/categories | python3 -m json.tool
# Attendu : tableau JSON avec au moins 8 catégories hardcodées (GEOGRAPHY, ENTERTAINMENT, etc.)

# 5.2 — POST /api/categories (valid)
curl -s -X POST http://localhost/api/categories \
  -H "Content-Type: application/json" \
  -d '{"name": "Test Smoke"}' | python3 -m json.tool
# Attendu : {"key":"TEST_SMOKE","name":"Test Smoke","imageURL":"","isCustom":true}

# 5.3 — POST /api/categories (conflict)
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost/api/categories \
  -H "Content-Type: application/json" \
  -d '{"name": "Test Smoke"}'
# Attendu : 409

# 5.4 — POST /api/categories (empty name)
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost/api/categories \
  -H "Content-Type: application/json" \
  -d '{"name": ""}'
# Attendu : 400

# 5.5 — GET /backup-select?medias=true
curl -s "http://localhost/backup-select?medias=true" -o /tmp/test.tar && tar -tvf /tmp/test.tar | head -20
# Attendu : TAR valide avec files/backgrounds/ et/ou files/categories/ si présents

# 5.6 — GET /reset-select?medias=true (dry-run : vérifier la réponse JSON)
curl -s "http://localhost/reset-select?medias=true" | python3 -m json.tool
# Attendu : {"status":"ok","reset":{"medias":true}}
# NOTE : ce test efface réellement les médias — ne pas exécuter sur un environnement de prod sans backup
```

| Étape | Code HTTP / Contenu attendu | Résultat Obtenu | OK ? |
|-------|----------------------------|----------------|------|
| 5.1 | 200, tableau avec GEOGRAPHY | | |
| 5.2 | 200, key=TEST_SMOKE, isCustom=true | | |
| 5.3 | 409 | | |
| 5.4 | 400 | | |
| 5.5 | TAR valide | | |
| 5.6 | `{"status":"ok","reset":{"medias":true}}` | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Critères de Validation Globale

- [ ] Bouton "+" visible et fonctionnel dans QuestionsPage
- [ ] Formulaire inline apparaît/disparaît correctement
- [ ] Erreurs 400/409 bien gérées avec messages lisibles
- [ ] Auto-sélection de la nouvelle catégorie après création
- [ ] Bouton "Speedy" actif par défaut (remplace "Normal")
- [ ] Fichiers TYPE=NORMAL servis comme TYPE=SPEEDY (migration transparente)
- [ ] Label "Médias & Catégories" dans BackupPage (remplace "Fonds")
- [ ] `medias=true` dans backup-select inclut backgrounds + categories
- [ ] `medias=true` dans reset-select retourne `result.medias=true`
- [ ] Aucune régression sur les features précédentes (categories listing, backup complet, QCM, MEMORY)

---

## Notes QA

[Espace pour observations]
