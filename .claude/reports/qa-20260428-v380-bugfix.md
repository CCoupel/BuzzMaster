# Rapport QA — v3.8.0 bugfix double WebSocket (111d184)

**Date** : 2026-04-28  
**Branche** : `feature/ws-broadcast-ack-v380`  
**SHA testé** : `111d184`  
**Scope** : `App.jsx` — restriction `GameProvider` aux routes `/admin/*` et `/anim/*`  
**Agent QA** : qa

---

## Résumé Exécutif

| Vérification | Résultat | Détails |
|-------------|----------|---------|
| Build React | ✅ PASS | `✓ built in 4.57s` — exit 0 |
| Tests React | ✅ PASS | 119/131 — baseline respecté (12 OOM ConfigPage pré-existant) |
| Analyse statique `PlayerDisplay.jsx` | ✅ PASS | Aucun `useGame` / `GameContext` / `GameProvider` |
| Analyse statique `VPlayerPage.jsx` | ✅ PASS | Aucun `useGame` / `GameContext` / `GameProvider` |
| Analyse statique `EnrollPage.jsx` | ✅ PASS | Aucun `useGame` / `GameContext` / `GameProvider` |
| Analyse statique `App.jsx` | ✅ PASS | `GameProvider` restreint aux routes admin uniquement |
| Build Go (non-régression) | ✅ PASS | `go build ./...` — exit 0 |

---

## Verdict

```
[X] VALIDATED
[ ] VALIDATED WITH RESERVATIONS
[ ] NOT VALIDATED
```

Aucune régression détectée. Le bugfix est conforme au contrat attendu.

---

## Vérification Statique App.jsx

Extrait pertinent de `server-go/web/src/App.jsx` :

```
Ligne 73 : // App root — GameProvider only instantiated for admin routes
Ligne 77 : const isAdminRoute = location.pathname.startsWith('/admin') || location.pathname.startsWith('/anim')
Ligne 81 : <GameProvider>       ← actif uniquement si isAdminRoute
Ligne 83 : </GameProvider>
Ligne 87 : // Non-admin routes: no GameProvider → no parasitic /ws/admin connection
```

`GameProvider` est instancié conditionnellement via `isAdminRoute`. Les routes `/tv`, `/player`, `/` sont rendues hors du `GameProvider` → aucune connexion `/ws/admin` parasite.

---

## Tests React — 119/131 PASS (baseline maintenu)

| Fichier | Tests | Statut |
|---------|-------|--------|
| `colorUtils.test.js` | 30/30 | ✅ |
| `TeamCard.test.jsx` | 9/9 | ✅ |
| `GamePage.categories.test.jsx` | 12/12 | ✅ |
| `TeamsPage.test.jsx` | 14/14 | ✅ |
| `GamePage.test.jsx` | 22/22 | ✅ |
| `USBConfigModal.test.jsx` | 18/18 | ✅ |
| `BackupPage.test.jsx` | 14/14 | ✅ |
| `ConfigPage.test.jsx` | 0/12 | ⚠️ OOM env WSL (pré-existant) |

Aucune régression par rapport à la baseline `044d74f` (119/131).

---

## Builds

- **React** : `npm run build` → `✓ built in 4.57s` — SUCCESS
- **Go** : `go build ./...` → exit 0 — SUCCESS

---

## Commit vérifié

```
SHA  : 111d184
Msg  : fix(#11): restrict GameProvider to admin routes — no double WS
Files: server-go/web/src/App.jsx (+29/-28)
```

Un seul fichier modifié, scope minimal, conforme à la description du bugfix.
