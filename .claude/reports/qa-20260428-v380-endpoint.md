# Rapport QA — v3.8.0 fix GameProvider endpoint (8021be1)

**Date** : 2026-04-28  
**Branche** : `feature/ws-broadcast-ack-v380`  
**SHA testé** : `8021be1`  
**Scope** : `GameContext.jsx`, `App.jsx`, `PlayerDisplay.jsx`, `VPlayerPage.jsx`, `EnrollPage.jsx`  
**Agent QA** : qa

---

## Résumé Exécutif

| Vérification | Résultat | Détails |
|-------------|----------|---------|
| Build React | ✅ PASS | `✓ built in 3.43s` — exit 0 |
| Tests React | ✅ PASS | 119/131 — baseline maintenu, 0 régression |
| `GameContext.jsx` — endpoint → useWebSocket | ✅ PASS | Confirmé |
| `App.jsx` — endpoint par route, un seul GameProvider | ✅ PASS | Confirmé |
| `PlayerDisplay.jsx` — useGame() uniquement | ✅ PASS | Aucun useWebSocket direct |
| `VPlayerPage.jsx` — useGame() uniquement | ✅ PASS | Aucun useWebSocket direct |
| `EnrollPage.jsx` — useGame() uniquement | ✅ PASS | Aucun useWebSocket direct |
| Build Go (non-régression) | ✅ PASS | `go build ./...` — exit 0 |

---

## Verdict

```
[X] VALIDATED
[ ] VALIDATED WITH RESERVATIONS
[ ] NOT VALIDATED
```

Aucune régression. Fix conforme au contrat attendu.

---

## Vérification Statique

### GameContext.jsx

```
Ligne 6 : export function GameProvider({ children, endpoint = '/ws/admin' }) {
Ligne 7 :   const websocket = useWebSocket(endpoint)
```
Prop `endpoint` acceptée avec défaut `/ws/admin`, transmise directement à `useWebSocket`. ✅

### App.jsx

```
Ligne 91 : const endpoint = isAdminRoute ? '/ws/admin' : isTvRoute ? '/ws/tv' : '/ws/player'
Ligne 94 : <GameProvider endpoint={endpoint}>
Ligne 96 : </GameProvider>
```
Un seul `GameProvider` enveloppe toutes les routes. L'endpoint est sélectionné par route :
- `/admin/*`, `/anim/*` → `/ws/admin`
- `/tv` → `/ws/tv`
- `/player`, `/enroll`, `/` → `/ws/player`

Chaque page obtient exactement une connexion WS via le contexte. ✅

### PlayerDisplay.jsx, VPlayerPage.jsx, EnrollPage.jsx

| Fichier | Import | Usage | useWebSocket direct |
|---------|--------|-------|-------------------|
| `PlayerDisplay.jsx` | `import { useGame } from '../hooks/GameContext'` | `useGame()` | ❌ Absent |
| `VPlayerPage.jsx` | `import { useGame } from '../hooks/GameContext'` | `useGame()` | ❌ Absent |
| `EnrollPage.jsx` | `import { useGame } from '../hooks/GameContext'` | `useGame()` | ❌ Absent |

Les 3 pages passent exclusivement par `useGame()` — aucun appel direct à `useWebSocket`. ✅

---

## Tests React — 119/131 PASS (baseline maintenu)

| Fichier | Tests | Statut |
|---------|-------|--------|
| `colorUtils.test.js` | 30/30 | ✅ |
| `TeamCard.test.jsx` | 9/9 | ✅ |
| `GamePage.categories.test.jsx` | 12/12 | ✅ |
| `GamePage.test.jsx` | 22/22 | ✅ |
| `TeamsPage.test.jsx` | 14/14 | ✅ |
| `USBConfigModal.test.jsx` | 18/18 | ✅ |
| `BackupPage.test.jsx` | 14/14 | ✅ |
| `ConfigPage.test.jsx` | 0/12 | ⚠️ OOM env WSL (pré-existant) |

Identique à la baseline — aucune régression introduite par ce commit.

---

## Builds

- **React** : `npm run build` → `✓ built in 3.43s` — SUCCESS
- **Go** : `go build ./...` → exit 0 — SUCCESS

---

## Commit vérifié

```
SHA   : 8021be1
Msg   : fix(#11): GameProvider endpoint prop — single WS per page, no crash
Files : App.jsx, GameContext.jsx, EnrollPage.jsx (+/- quelques lignes chacun)
```
