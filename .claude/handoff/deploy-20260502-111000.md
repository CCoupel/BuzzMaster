# Handoff — Deploy

**Feature** : Build QUALIF v4.0.8 — fix HTTP port retry on self-update
**SHA** : 65d06a5 (dernier commit — docs changelog)

## Ce qui a été fait

Build QUALIF v4.0.8 complet selon les règles BuzzControl (ordre BORE) :
1. Firmware BuzzClick compilé via PlatformIO (buzzclick env, SUCCESS en 26s)
2. Merge des binaires esptool.py → buzzclick-merged.bin (1.1 MB)
3. Assets Go mis à jour : `server-go/assets/firmware/buzzclick-latest.bin` + `version.txt` → 4.0.8
4. Frontend React buildé → `server-go/web/dist/` (636 KB main chunk, 195 KB gzip)
5. Cross-compilation Go Windows AMD64 → `server-go/buzzcontrol-qualif-amd64.exe` (17 MB)

## Décisions clés

- Firmware rebuilé car version.txt était à 4.0.7 (≠ 4.0.8 cible)
- Ordre BORE respecté : firmware → frontend → backend Go
- Artefact produit : `server-go/buzzcontrol-qualif-amd64.exe` (17 MB, binaire auto-suffisant)
- Warning Vite sur chunk size (636 KB) : non bloquant, comportement habituel du projet

## Points d'attention

- L'artefact QUALIF est `server-go/buzzcontrol-qualif-amd64.exe`
- Pour les smoke tests : démarrer le .exe sur Windows, vérifier `/api/version` et `/api/firmware/buzzclick/version`
- Le binaire embarque firmware 4.0.8 ET frontend React 4.0.8 (BORE garanti)
- Les assets firmware ont été mis à jour (buzzclick-latest.bin + version.txt) — ces fichiers ne sont pas commités (gitignore), ils sont régénérés à chaque build

## Fichiers modifiés/produits

- `server-go/assets/firmware/buzzclick-latest.bin` (rebuild)
- `server-go/assets/firmware/version.txt` (4.0.7 → 4.0.8)
- `server-go/web/dist/` (frontend buildé)
- `server-go/buzzcontrol-qualif-amd64.exe` (artefact QUALIF)
