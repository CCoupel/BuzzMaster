# Handoff — TEST-WRITER

**Feature** : v3.8.0 — #11 WS endpoints dédiés + #41 payload buzzers + #54 ACK buzzer
**SHA** : 044d74f

## Ce qui a été fait

Analyse des tests existants (websocket_test.go, websocket_buzzer_test.go, ack_manager_test.go) — tous passaient déjà pour les cas de base. Ajout de 3 tests Go manquants par rapport aux contrats API, création de TeamCard.test.jsx (inexistant), extension de TeamsPage.test.jsx pour ACK_PENDING, et création de la procédure manuelle QA en 7 scénarios couvrant les 3 issues.

## Décisions clés

1. **Non-régression stricte** : aucun test existant modifié. Tous les ajouts sont additifs.
2. **Go : 3 cas complémentaires** : QUESTIONS→admin-only, BACKGROUND_CHANGE→admin+tv (cas non couverts par les tests backend bien que le code implémentait correctement ces filtres), et concurrent AckManager (test -race).
3. **React TeamCard.test.jsx créé** : le fichier n'existait pas. Tests directs sur le composant réel (pas de mock TeamCard) avec framer-motion aliasé globalement via vite.config.js. Badge identifié par `title="En attente de confirmation"`.
4. **TeamsPage.test.jsx** : nouveau describe bloc ajouté. Le badge dans TeamsPage utilise la même condition (`ACK_PENDING === true && !IS_VIRTUAL && !IS_VPLAYER`) — testé dans les 2 sections (non assignés et équipes).

## Points d'attention pour QA

- **Tests Go** : lancer `go test ./internal/server/ -race -run TestAckManager_ConcurrentAccess` pour valider le test de concurrence avec le race detector.
- **Tests React** : `TeamCard.test.jsx` importe le vrai composant TeamCard — si de nouvelles dépendances sont ajoutées à TeamCard.jsx, le test devra être mis à jour.
- **Procédure manuelle** : le scénario 6 (ACK timeout) est plus facile à tester avec `ack_max_retries=1` et `ack_timeout_ms=500` dans config.json pour accélérer le cycle.
- **Scénario 4** : nécessite un buzzer avec firmware v3.8.0+. Sans firmware ACK, utiliser le scénario 6 à la place.

## Fichiers modifiés

- `server-go/internal/server/websocket_test.go` — +2 tests (#11 QUESTIONS/BACKGROUND_CHANGE)
- `server-go/internal/server/ack_manager_test.go` — +1 test (#54 concurrent access) + imports fmt/sync
- `server-go/web/src/pages/TeamsPage.test.jsx` — +describe ACK_PENDING (7 tests)
- `server-go/web/src/components/TeamCard.test.jsx` — NEW (9 tests badge ACK_PENDING)
- `tests/procedures/v3.8.0-ws-broadcast-ack.md` — NEW (7 scénarios QA manuels)
- `.claude/handoff/test-writer-20260428-v380.md` — ce fichier
