# Handoff — Deploy QUALIF v4.0.7

**Feature** : Build QUALIF BuzzControl v4.0.7
**SHA** : f84b905

## Ce qui a été fait

Build QUALIF complet exécuté selon l'ordre BuzzControl obligatoire (BORE) :
1. Firmware BuzzClick compilé via PlatformIO (esp32c3, 18s) et mergé (bootloader+partitions+boot_app0+app → 1.1MB)
2. Firmware merged intégré dans `server-go/assets/firmware/` avec version.txt=4.0.7
3. Frontend React buildé via `npm run build` (Vite, 3s, 497 modules)
4. Backend Go cross-compilé Windows AMD64 (GOOS=windows GOARCH=amd64, CGO_ENABLED=0) → 17MB

## Décisions clés

- Firmware était en v4.0.1 dans les assets → recompilation obligatoire (pas de skip)
- Build Go avec `-ldflags="-s -w"` pour réduire la taille du binaire
- Ordre BORE respecté : firmware → React → Go (ordre critique pour l'embedded)

## Points d'attention

- L'artefact `buzzcontrol-qualif-amd64.exe` (17MB) est prêt pour les tests QUALIF Windows
- Le firmware merged v4.0.7 est embarqué dans le binaire serveur
- Smoke tests non exécutés (serveur non démarré — QUALIF validation à faire manuellement)
- Avertissement Vite sur chunk > 500KB (index.js = 636KB) — non bloquant, connu

## Fichiers produits

- `server-go/buzzcontrol-qualif-amd64.exe` — artefact QUALIF Windows (17MB)
- `server-go/assets/firmware/buzzclick-latest.bin` — firmware merged v4.0.7 (1.1MB)
- `server-go/assets/firmware/version.txt` — "4.0.7"
- `server-go/web/dist/` — frontend React buildé (non commité, embarqué dans .exe)
