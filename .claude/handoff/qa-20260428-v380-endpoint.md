# Handoff — QA

**Feature** : v3.8.0 fix GameProvider endpoint — single WS per page, no crash
**SHA** : 8021be1

## Ce qui a été fait

Vérification complète du fix GameProvider endpoint : analyse statique des 5 fichiers concernés, build React, tests React (baseline), build Go non-régression. Verdict VALIDATED sans réserve.

## Décisions clés

1. GameContext.jsx : prop `endpoint` transmise à `useWebSocket` — pattern correct.
2. App.jsx : endpoint calculé par route, un seul GameProvider enveloppe tout — pas de double connexion.
3. PlayerDisplay/VPlayerPage/EnrollPage : uniquement `useGame()`, aucun `useWebSocket` direct — crash "must be within GameProvider" éliminé.
4. Tests 119/131 identiques à la baseline des 2 QA précédentes — stabilité confirmée.

## Points d'attention

- Aucun. Fix propre, scope maîtrisé.

## Fichiers modifiés

- `.claude/reports/qa-20260428-v380-endpoint.md` — rapport QA
- `.claude/handoff/qa-20260428-v380-endpoint.md` — ce fichier
