# Handoff — QA

**Feature** : v3.8.0 — #11 WS endpoints dédiés + #41 payload buzzers + #54 ACK buzzer
**SHA** : 044d74f

## Ce qui a été fait

Exécution complète des suites de tests unitaires Go (444 tests, 5 packages) et React (119/131), validation des builds Go et React, analyse statique des 3 corrections MAJEURES identifiées par le reviewer. Rapport QA généré avec verdict VALIDATED WITH RESERVATIONS.

## Décisions clés

1. **Les 3 MAJEURs sont confirmés corrigés** : BroadcastToTypes utilise WLock (MAJEUR-1), OnRetry réutilise le msgID original (MAJEUR-2), sendLEDSet n'appelle plus broadcastUpdate (MAJEUR-3). Corrections confirmées par analyse statique + commentaires explicites dans le code.
2. **OOM ConfigPage.test.jsx** : Problème d'environnement WSL (Node Windows via interop). 12 tests non exécutés. ConfigPage.test.jsx est pré-existant et non modifié par v3.8.0. Non bloquant.
3. **Race detector non exécutable** : Pas de gcc dans WSL. Analyse statique WLock + test ConcurrentAccess sans -race compensent. À valider en CI.
4. **Couverture 42.6%** : Pré-existante, stable. Acceptable pour ce projet.

## Points d'attention

- Race detector à valider en CI GitHub Actions (Linux + gcc) lors du push de la PR
- Procédure manuelle E2E (7 scénarios) à exécuter avant mise en PROD
- OOM ConfigPage à investiguer (Vitest config `--pool=forks` ou `--single-thread`)
- 4 points mineurs du reviewer restent ouverts pour un commit post-merge

## Fichiers modifiés

- `.claude/reports/qa-20260428-v380.md` — rapport QA
- `.claude/handoff/qa-20260428-v380.md` — ce fichier
